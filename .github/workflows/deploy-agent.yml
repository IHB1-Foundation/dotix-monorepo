name: Deploy Agent (Railway)

# Railway deployment is done via CLI: `railway up`
# Project: dotix-agent, Service: plan-api
# This workflow is kept for reference only.

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Agent deploys via 'railway up' from local CLI."
