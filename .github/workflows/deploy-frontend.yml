name: Deploy Frontend (Vercel)

# Vercel Git Integration handles auto-deploy on push to main.
# This workflow is kept for manual trigger only.
# Vercel project "dotix" is connected to this repo via Git Integration.
# Root directory: app/

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Frontend deploys automatically via Vercel Git Integration on push to main."
